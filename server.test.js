test("string with a single number should result in the number itself", () => {
  const one = 1;
  expect(one).toBe(1);
});

test("string with one number should result in the number itself", () => {
  const one = 1;
  expect(one).toBe(1);
});

test("string with one  should result in the number itself", () => {
  const one = 1;
  expect(one).toBe(1);
});
